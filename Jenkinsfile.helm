pipeline {
    agent any

    stages {
        stage('Test') {
            steps {
                sh '''
                #!/bin/bash
                helm list
                echo 'Hello World'
                '''
            }
        }
    }
}
