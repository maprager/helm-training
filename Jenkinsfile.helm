pipeline {
    agent any

    stages {
        stage('Test') {
            steps {
                sh '''
                #!/bin/bash
                pwd
                ip a
                helm list
                echo 'Hello World'
                '''
            }
        }
    }
}
